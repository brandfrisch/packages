---
ansible:
  playbook: playbook.yml

docker:
  containers:
    - name: packages
      image: brandfrisch/ubuntu-16.04
      image_version: 'latest'
      command: /lib/systemd/systemd
      privileged: true

verifier:
  name: goss